services:
  - type: web
    runtime: node
    name: medpremium-api
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
